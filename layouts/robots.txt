{{/*
    自定义 robots.txt

    全站门禁开启时禁止抓取：门禁虽拦得住普通访客，但爬虫不执行 JS，
    会照常抓到页面骨架与标题。既然整站要求输密码才进，
    就不该出现在搜索结果里。

    加密文章的正文本身是密文，即便被抓也不泄露正文
    （搜索索引与 meta description 亦已排除，见
     layouts/page/search.json 与 _partials/data/description.html）。
    这里禁止收录是为了连标题都不外露。

    注意 robots.txt 只是「君子协定」：守规矩的爬虫会遵守，
    恶意爬虫不会。真正的保密由内容加密承担。
*/}}
{{- if .Site.Params.encrypt.gate -}}
User-agent: *
Disallow: /
{{- else -}}
User-agent: *
Allow: /

Sitemap: {{ "sitemap.xml" | absURL }}
{{- end -}}
