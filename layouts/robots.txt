{{/*
    自定义 robots.txt

    内容由加密保护（正文在 HTML 中是密文），因此允许收录：
    搜索引擎只能抓到标题与日期，抓不到正文。
    加密文章的搜索索引与 meta description 也已排除正文
    （见 layouts/page/search.json 与 _partials/data/description.html）。

    若改回 JS 门禁模式（params.gate.enable = true），则禁止全站抓取 ——
    那种模式下正文是明文，被收录等于经搜索结果泄露。
*/}}
{{- if .Site.Params.gate.enable -}}
User-agent: *
Disallow: /
{{- else -}}
User-agent: *
Allow: /

Sitemap: {{ "sitemap.xml" | absURL }}
{{- end -}}
